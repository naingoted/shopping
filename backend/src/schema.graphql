# import * from './generated/prisma.graphql' 
# this import is necessary for "Item"


type Mutation {
  createItem(title: String, description: String, price: Int, image: String, largeImage: String): Item!
}

type Query {
  items: [Item]!
}